import 'package:flutter/material.dart';

/// Only used when bock actor is provided
class DeleteAlertDialogue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return AlertDialog();
  }
}
